## Summary

What changed and why.

## Verification

- [ ] `npm run lint`
- [ ] `npm run typecheck`
- [ ] `npm test`
- [ ] `npm run probe` — runs `build` first, then the only check that actually activates the extension; CI's build job runs this too

## OpenSpec

- [ ] An `openspec/changes/` folder applies and is included
- [ ] N/A — no spec-level behavior changed

## Notes

- [ ] Non-obvious fix, pattern, or decision — recorded in `docs/` or `docs/agent-notes/`
- [ ] N/A — straightforward change
